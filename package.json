{
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "vercel-build": "vite build && php artisan config:clear && php artisan cache:clear && php artisan route:clear && php artisan view:clear"
  },
  "devDependencies": {
    "axios": "^1.1.2",
    "laravel-vite-plugin": "^0.7.2",
    "vite": "^4.0.0"
  }
}
